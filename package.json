{
  "name": "article-actualization-ui",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "diff": "^9.0.0",
    "vitest": "^2.1.0"
  }
}
